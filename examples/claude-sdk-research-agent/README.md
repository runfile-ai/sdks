# Example — Claude Agent SDK research agent

End-to-end demo: a Claude Agent SDK research agent instrumented via the Runfile
hooks + `canUseTool` adapter, showing tool-approval capture, subagent
delegation, and bundle verification.

🚧 Scaffold — to be filled in once the Claude Agent SDK adapter lands (Python and
TypeScript both ship in v1).
